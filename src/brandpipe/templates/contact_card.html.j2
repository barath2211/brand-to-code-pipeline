<section class="bp-card" aria-labelledby="ct-{{ brand.client_id }}">
  <h2 id="ct-{{ brand.client_id }}">Investor relations contact</h2>
  <p>{{ content.contact.name }}<br><span class="bp-muted">{{ content.contact.role }}</span></p>
  <button class="bp-btn" type="button">Subscribe to alerts</button>
</section>
